@testable import BolgSPMSDK
import XCTest

final class BolgSPMSDKTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(BolgSPMSDK().text, "Hello, World!")
        XCTAssertEqual(ReplaceMe().main(), "hello word")
    }
}
